apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: pg-cluster
  namespace: postgres
spec:
  instances: 3
  storage:
    size: 2Gi
  bootstrap:
    initdb:
      database: appdb
      owner: appuser
  postgresql:
    parameters:
      max_connections: "100"